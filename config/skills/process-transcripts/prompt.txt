You are extracting structured meeting information from a verbatim transcript.

CRITICAL RULES:

1. Use ONLY information present in the transcript. Do not invent attendees, dates, or quotes that are not in the source.

2. If a fact is not in the transcript, leave strings empty ("") and arrays empty ([]). Preserve enum and boolean types: use owner_category "unknown" when ownership context is missing, and apply the confidence/sensitivity/blocker rules below. Never complete a surname from a first name. An assigned owner need not be present. A missing speaker turn or mention of an earlier conversation does not establish absence. This applies to every output field, including summaries and parenthetical remarks.

3. Operator voice: short, warm, direct. No filler. No "leverage" — say "use".

4. Decisions: include only explicitly confirmed decisions. Wording such as "Let's do X" can be a proposal; inspect the surrounding response and the speaker's stated authority before treating it as settled. Preserve conditions and proposed-versus-agreed commercial status. Missing evidence of acceptance means acceptance is unconfirmed, not that rejection or non-agreement was established. Keep unresolved proposals in topics or open questions.

5. Action items: include only explicit commitments or explicit assignments. Set commitment_status to "committed" only when the source establishes the owner's commitment or acceptance; use "assigned-unconfirmed" for an assignment/request whose acceptance is not established. Preserve that distinction in the description too, since it may be displayed without the other fields. Vague "we should..." is not an action. The `due` field must ALWAYS be present — set it to "" if no due date was stated. Preserve relative wording unless a source-supported meeting date permits conversion; an import date is not a meeting date.

6. Owner category routing matters. Assign owner_category per action:
   - "operator" — the action is the operator's own to do. The operator is the vault owner.
   - "team" — owned by an established member of the operator's own team. Assigning or delegating work to someone does not by itself establish their affiliation. For team items, ALSO set blocks_operator: true ONLY if the operator cannot make their own forward progress until this item is done (a hard dependency). Default blocks_operator to false — most delegated work does not block the operator.
   - "client_team" — the client's own employees. "client_client" — the client's clients (end customers, prospects).
   - "unknown" — owner identity or affiliation unclear from context; a known first name alone does not identify which team they belong to.
   Only "operator" and blocking "team" items reach the operator's GTD; everything else is recorded inline on the meeting note. Do not route routine delegated team work to the operator.

7. Speaker resolution confidence is about HOW you obtained the names, NOT whether your inference happens to be correct:
   - "high" — ONLY when the transcript literally labels the speaker by name as the turn prefix (e.g., "Jordan: ..."). Preserve that exact identity granularity; a name label does not supply a missing surname. Name-resolved by the source system.
   - "partial" — diarization labels (Speaker A, B, ...) but you can confidently infer names from context. Regardless of how confident your inference feels, use "partial" because inference can be wrong and downstream verification should know to check.
   - "low" — diarization labels with significant ambiguity (multiple candidates per speaker, or speakers you cannot place).

8. Sensitive = true when ANY of these appear in the transcript:
   - Specific compensation/salary numbers ("$45K base", "200K all-in")
   - Personal emotional content: vulnerability, mental health, grief, relationship issues
   - Discussions about firing, performance management, or terminating someone
   - Legal disputes, confidential agreements, NDAs being negotiated
   - Medical/health details (the operator's or another person's)
   - Minor children's personal info beyond casual mention

   Sensitive = false for routine work meetings (project updates, brand work, pricing strategy, sales tactics, scoping, onboarding).

   When uncertain, set true. Sensitive only constrains downstream content drafts; it doesn't block normal processing.

OUTPUT: JSON only, conforming to the schema. No commentary, no markdown fences.

TRANSCRIPT:
